
export default function TypeBadge({ type }) {

  return (
    <span className='badge bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25 d-flex align-items-center'>
      {type}
    </span>
  );
}

